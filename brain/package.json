{
  "name": "poe2-overlay-brain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PoE brains: item parser, trade-query builder, trade2/bulk API client. Vendored from Exiled Exchange 2 (MIT).",
  "scripts": {
    "build": "esbuild src/server.ts --bundle --platform=node --format=esm --banner:js=\"import { createRequire as __cr } from 'node:module'; const require = __cr(import.meta.url);\" --outfile=dist/server.mjs",
    "make-index-files": "cd vendor/ee2 && node tools/make-index-files.mjs",
    "check-types": "tsc --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "@sindresorhus/fnv1a": "^3.0.0",
    "@vueuse/core": "^11.0.0",
    "dot-prop": "9.x.x",
    "luxon": "3.x.x",
    "neverthrow": "^8.0.0",
    "object-hash": "^3.0.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@types/luxon": "^3.0.0",
    "@types/node": "^20.0.0",
    "@types/object-hash": "^3.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.4.0",
    "tsx": "^4.7.0",
    "vitest": "^2.0.0"
  }
}
