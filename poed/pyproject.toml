[project]
name = "poed"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["PyGObject"]

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
